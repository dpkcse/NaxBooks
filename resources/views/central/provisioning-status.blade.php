@extends('layouts.app')
@section('content')<section class="mx-auto max-w-2xl rounded-2xl bg-white p-8 shadow"><h1 class="text-2xl font-bold">Provisioning {{ $tenant->name }}</h1><p class="mt-2 text-slate-600">{{ $attempt?->status?->value ?? 'pending' }} · {{ $attempt?->current_step ?? 'account_created' }}</p><p class="mt-4 text-sm">{{ count($attempt?->completed_steps ?? []) }} of 7 setup steps complete.</p>@if($attempt?->sanitized_error_message)<p class="mt-4 rounded bg-red-50 p-3 text-red-800">{{ $attempt->sanitized_error_message }} Contact support or ask a platform administrator to retry.</p>@endif @if($tenant->status->value==='trialing')<a class="mt-6 inline-block rounded bg-indigo-700 px-4 py-2 text-white" href="{{ config('app.url') }}/login">Open Workspace (sign in on your workspace)</a>@else<p class="mt-6 text-sm text-slate-500">This page refreshes safely while provisioning continues.</p>@endif</section>@endsection
