@props(['title','items'=>[]])<section><h2 class="px-2 text-xs font-semibold uppercase tracking-wide text-slate-500">{{ $title }}</h2><div class="mt-2 space-y-1">@foreach($items as $item)<a href="{{ $item === 'Dashboard' ? route('dashboard') : '#' }}" class="block rounded-lg px-3 py-2 text-sm font-medium text-slate-700 hover:bg-slate-100 focus-visible:bg-slate-100" @if($item !== 'Dashboard') aria-disabled="true" @endif>{{ $item }}</a>@endforeach</div></section>
