@extends('layouts.app')@section('content')<h1>Reset password</h1><form method="POST" action="{{ route('password.update') }}">@csrf<input type="hidden" name="token" value="{{ $request->route('token') }}"><input name="email" type="email" value="{{ old('email', $request->email) }}" required><input name="password" type="password" required><input name="password_confirmation" type="password" required><button>Reset password</button></form>@endsection
